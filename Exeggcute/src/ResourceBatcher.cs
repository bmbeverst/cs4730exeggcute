using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Exeggcute.src.entities;

namespace Exeggcute.src
{
    /*class ResourceBatcher
    {

        //maps batch ID's to lists of entitys
        public Dictionary<int, List<int>> batchAssignment = new Dictionary<int, List<int>>();
        public void UpdateBatch<T>(T asset, int batchID)
        {
            foreach (List<int> list in batchAssignment[batchID])
            {
                foreach (int id in list)
                {
                    Worlds.World.Entities[ID]//.setAsset(whichasset, asset)
                }
                
            }
        }
        //update scripts
        //reset entities
        public void UpdateAll<T>(T asset)
        {
            for (int i = 0; i < batches.Length; i += 1)
            {
                foreach (int id in batchAssignment[i])
                {
                    Worlds.World.Entities[id]//.setAsset(whichasset, asset)
                }
            }
        }
    }*/
}
